export enum UserVerifyStatus {
  Unverified,
  Verified,
  Baned
}

export enum TokenType {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerifyToken
}

export enum MediaType {
  Image,
  Video
}

export enum EncodingStatus {
  Pending,
  Processing,
  Success,
  Failed
}
